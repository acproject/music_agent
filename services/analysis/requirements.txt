# M0：gRPC 通信与数值基础
# 版本约束说明：*_pb2.py 由 grpcio-tools（捆绑 protoc）生成，protobuf 运行时
# 不得旧于生成代码版本（gencode），否则启动即报 VersionError。
# 三者须一同升级后重新生成桩：pip install -U grpcio grpcio-tools protobuf
# 再执行 node scripts/gen_proto.mjs python。当前已测矩阵：1.84.0 / 1.84.0 / 7.35.1。
grpcio>=1.84.0
grpcio-tools>=1.84.0
protobuf>=7.35.1
numpy>=1.26

# M3：离线高质量音高后端 CREPE（ONNX Runtime CPU 版，无需 torch）。
# 懒加载：仅 PITCH_BACKEND=crepe 时导入；不装也不影响默认 YIN 与实时链路。
# 权重不随仓库分发，需另跑：python scripts/download_model.py --models tiny
onnxruntime>=1.17

# M2 已实现：app/pitch.py 内为纯 numpy YIN 实时音高（40ms/帧，零原生依赖）
#
# 后续可选增强（暂不安装）：
# aubio           # C 核心实时音高/onset，作为 YIN 的加速/对照实现
# librosa         # 高质量分析、节拍、色度
# note-seq / basic-pitch  # 音频转 MIDI
# pretty-midi     # MIDI 生成
# music21         # MusicXML / 五线谱
