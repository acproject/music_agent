# Rust
/target

# Node / Web
node_modules
dist
apps/web/src/proto/**/*.ts

# Python（proto 生成物不入库，由 scripts/gen_python_proto.sh 生成）
__pycache__/
*.py[cod]
.venv/
venv/
services/analysis/app/proto/

# 运行时数据 / 密钥
/data
.env

# 模型权重（体积大，由 scripts/download_model.py 按需下载）
/models/

# 本地启停脚本产物（日志 / PID）
/logs/
/.run/

# IDE / OS
.idea/
.vscode/
.DS_Store
.env
